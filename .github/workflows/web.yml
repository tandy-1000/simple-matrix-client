name: Web deployment
on:
  push:
    paths:
      - ".github/workflows/**"
      - "simple_matrix_client/**"
      - "**.nim"
      - "public/**"
      - "simple_matrix_client.nimble"

jobs:
  build:
    strategy:
      fail-fast: false

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2.4.0

      - name: Setup Nim
        uses: alaviss/setup-nim@0.1.1
        with:
          path: nim
          version: devel

      - name: Setup Dart Sass, UglifyJS, html-minifer
        run: npm install -g sass uglify-js html-minifier

      - name: Install dependencies
        run: nimble install -d:js -y --depsOnly

      - name: Build Sass
        run: nimble sass

      - name: Compile release build
        run: nimble buildjs

      - name: Minify JS
        run: uglifyjs public/js/simple_matrix_client.js -c -o public/js/simple_matrix_client.js

      - name: Minify HTML
        run: html-minifier --collapse-whitespace --remove-optional-tags --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype public/index.html -o public/index.html && html-minifier --collapse-whitespace --remove-optional-tags --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype public/mirror/index.html -o public/mirror/index.html

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.7
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: public # The folder the action should deploy.